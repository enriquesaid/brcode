name: brcode
description: Generates Pix code.
version: 0.0.2
homepage: https://github.com/enriquesaid/brcode/
repository: https://github.com/enriquesaid/brcode

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  crc: ^0.0.2
  crclib: ^3.0.0
  diacritic: ^0.1.4
  luthor: ^0.2.2
  slugify: ^2.0.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
